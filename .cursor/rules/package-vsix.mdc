---
description: VSIX 打包必须递增版本号
globs:
alwaysApply: true
---

打 VSIX 时：

1. 使用 `npm run package` 或 `npm run vsix`（二者等价）。
2. 禁止跳过 `scripts/bump-version.mjs` 直接运行 `vsce package`。
3. 功能性改动后，在 `CHANGELOG.md` 顶部补充新版本说明。
4. README 截图须能被扩展详情页加载：`npm run package` 已带 `--baseImagesUrl` 指向 `raw.githubusercontent.com/.../main/`，勿去掉（默认 `github.com/.../raw/HEAD` 在 VS Code 里会裂图）。
