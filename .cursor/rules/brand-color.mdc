---
description: OpenCode for VS Code 社区版品牌主色——透亮天蓝色
alwaysApply: true
---

# 品牌主色：夏天晴朗天空的天蓝色

本项目的视觉主色是**透亮的天蓝色**——像夏天晴朗天空那样清亮、通透，不是深蓝、不是紫蓝、不是灰蓝。

## 色值（已在代码中使用）

在 `webview/src/styles.css` 中通过 CSS 变量统一引用，**不要硬编码散落的颜色**：

| 变量 | 色值 | 用途 |
|------|------|------|
| `--sky` | `#38bdf8` | 主色：按钮、焦点环、药丸前缀、连接状态、强调 |
| `--sky-strong` | `#0284c7` | 悬停/按下、主按钮深色 |
| `--sky-soft` | `color-mix(in srgb, var(--sky) 16%, transparent)` | 轻背景、悬停底 |

## UI 原则

- 关键操作（发送、选中、连接成功、品牌标识）用 `--sky` / `--sky-strong`
- 大面积背景仍以 VS Code 主题变量为主；天蓝作**点缀与引导**，保持通透感
- 高对比度/无障碍优先：天蓝不能牺牲可读性
- 新增组件时复用现有 `--sky*` 变量，保持与 Composer、工具栏、消息流一致

## 参考

- 产品需求：`docs/产品需求文档.md`（品牌识别、天蓝色关键操作）
- 实现：`webview/src/styles.css` 的 `:root` 与 `.composer__*` 等
