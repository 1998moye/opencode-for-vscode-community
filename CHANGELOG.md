# 更新记录

## 0.0.140

- 修复扩展详情 README 截图不显示：`vsce package` 增加 `--baseImagesUrl`（`raw.githubusercontent.com`），避免默认 `github.com/raw/HEAD` 裂图。

## 0.0.139

- README 截图放入 `media/screenshots/` 并打入 VSIX。

## 0.0.138

- README 功能截图改为 GitHub `raw` 绝对链接；VSIX 打包包含 `docs/images`。
- 发布者 `Dingzhen`；编辑器标题栏图标高对比度（0.0.137）。

## 0.0.137

- 编辑器标题栏命令图标改为深浅主题专用 SVG（天蓝高对比），修复与主题背景糊在一起的问题。

## 0.0.136

- 扩展发布者改为 `Dingzhen`（Marketplace 发布者 ID）。

## 0.0.135

- 活动栏与标题栏图标：官方 OpenCode「O」标识单色变体 + 聊天入口 SVG；扩展市场 `icon.png` 使用官方方形资源（见 `media/BRAND.md`）。
- 重写 README：安装与使用说明、功能截图、GitHub 地址与发布安全清单；完善 `.gitignore` 排除密钥与 `opencode.json`。

## 0.0.134

- 用户短消息气泡随内容收窄；「重新发送 / 编辑」与气泡分离并缩小操作区。

## 0.0.133

- 移除助手气泡「撤销文件变更」；审查条批量改为「撤销变更」。
- 变更审查按钮统一尺寸与对比度（刷新 / 全部保留 / 撤销变更及行内操作）。

## 0.0.132

- 与 0.0.131 相同：修复「全部保留」后下一轮变更不显示（保留锚点按消息/内容快照）。

## 0.0.131

- 修复「全部保留」后同一会话下一轮 Agent 变更不显示：保留锚点按 messageId/内容快照匹配，不再按路径永久隐藏。

## 0.0.130

- 与 0.0.129 相同：删除文件恢复写盘修复；变更审查标题栏批量「全部保留 / 全部回退」。

## 0.0.129

- 修复 Agent 删除文件后点「恢复」误判成功但未写盘：以 `fs.stat` 判断磁盘存在，写盘前创建父目录，失败时不再从列表移除。
- 变更审查条折叠时也在标题栏显示「全部保留 / 全部回退」；请 `npm run build` 或 F5 前执行 `build:webview` 以更新界面。

## 0.0.128

- 助手回复气泡：账本有关联文件时显示「撤销文件变更」，安全条件与批量回退一致（消息 revert 优先，否则逐文件）。

## 0.0.127

- 会话文件变更：单文件「保留」；工作区文件外部修改后自动防抖刷新账本。
- 全部回退：在列表项同属一条消息且磁盘未被用户改写时，优先调用 OpenCode 消息级 revert，失败则回退为逐文件三方回退。

## 0.0.126

- 会话文件变更：「全部保留」「全部回退」批量操作；单文件/批量回退遇磁盘冲突时打开只读对比（修改前↔当前、修改前↔Agent 修改后）而非直接失败。

## 0.0.125

- 待处理授权：「仅本次允许」仅在选中/悬停时实心高亮，方向键切换更明显；底部增加键盘操作说明。
- Windows：托管 OpenCode Server 停止时按端口清理进程树，扩展重载时回收遗留 `serve`（及 Bun 等子进程）。

## 0.0.124

- 待处理授权：键盘选中样式与鼠标悬停一致，不再使用单独外圈高亮。

## 0.0.123

- 待处理授权：焦点在输入框时仍可用方向键切换选项、Enter 确认，并高亮当前项。
- 会话文件变更：回退后从列表移除且刷新不再带回；Server/工具未给行数时推算 +/− 统计。

## 0.0.122

- 修复 Windows 下停止 OpenCode Server 只杀掉 cmd.exe 包装层、opencode.exe 被孤儿化残留的问题：改为直接 taskkill 终止整棵进程树。

## 0.0.117

- 扩展 shell 删除识别：unlink/rmdir/git rm/trash/find/shred、Python/Node 内联删除、链式 `&&`/`;`，并过滤通配符路径。

## 0.0.116

- 识别 PowerShell `Remove-Item -LiteralPath` / `del` 删除；Read 索引按文件名匹配绝对路径，便于恢复 `current_time.py` 类删除。

## 0.0.115

- 文件变更折叠条：SVG 箭头与标题对齐、对比度与「刷新」按钮可读性。
- 删除入账：`rm` 命令解析、Read 输出兜底、`removed` 等状态归一。

## 0.0.114

- 查看差异改为只读虚拟文档，关闭时不再提示保存。
- 回退「新建」文件时删除文件，而非清空内容；支持「已删除」文件入账与恢复（依赖 Server/工具提供删除前正文）。

## 0.0.113

- 会话文件变更条支持折叠：默认只显示「N 个文件」，点击展开列表（类似 Cursor）。

## 0.0.112

- 出现授权/提问卡片时强制滚到底部（即使用户此前上滑）；时间线新增步骤也会触发跟滚。

## 0.0.111

- 修复变更审查条在侧栏被压扁、文件列表看不见的问题；列表区域单独滚动。

## 0.0.110

- 修复带文件上下文发送时出现两条用户气泡（乐观消息与服务端消息按正文合并）。
- 用户消息中的文件附件卡片缩小，减少占位。

## 0.0.109

- 变更审查条仅在会话内确有文件变更时出现，不再常驻空状态占位。

## 0.0.108

- Issue 16：会话变更账本（Server diff + 工具分片）、Webview 审查条、VS Code 原生差异视图与三方安全单文件回退。

## 0.0.107

- 斜杠命令带参数时，参数换行与技能标签左缘对齐，不再缩进到标签右侧。

## 0.0.106

- 修复 `/grill-me` 等技能仍出现第二条用户气泡：识别 OpenCode 注入的「Base directory…」运行时信封并过滤。

## 0.0.105

- 修复执行技能时出现两条用户消息：技能 prompt 正文不再当作普通文字展示；去掉「超过 160 字即技能模板」的误判规则。

## 0.0.104

- 修复 `/` 补全列表中技能名与描述文字重叠：限制命令名列宽并允许描述列正确收缩省略。

## 0.0.103

- 修复新会话首条消息气泡为空：创建会话时不再清掉已展示的乐观用户消息。
- 修复超过 160 字的普通用户消息被误判为技能模板而显示空气泡。

## 0.0.102

- `/` 补全直接列出全部技能（与 CLI 一致），不再依赖 `/skills` 单独扫描。
- Tab / 回车 / 点击补全项只写入输入框（如 `/grilling `），再按回车发送；不再误开技能选择器。

## 0.0.101

- 修复 `/skills` 技能列表为空：改为优先调用 `app.skills`（与 CLI 一致，向上扫描至 git worktree），不再误用返回空列表的 v2 API。
- 增加 `opencode debug skill` CLI 回退与目录解析优化。

## 0.0.100

- 补齐 OpenCode CLI 的 `/skills` 命令：输入 `/` 可补全并在选择后打开技能选择器。
- 斜杠补全改为与 CLI 一致的左右布局：`/命令` 在左、说明在右。

## 0.0.99

- 修复带文字发送斜杠询问（如 `/init 这个技能是干嘛的`）仍出现两条用户气泡的问题。
- 服务端先返回仅 `/命令` 时，将乐观更新的完整文字合并进同一条消息。

## 0.0.98

- 单独发送 `/init` 时缩小命令胶囊与气泡内边距。
- 修复带文字发送时额外出现一条仅 `/命令` 的重复气泡。

## 0.0.97

- 缩小带附加文字的斜杠命令胶囊尺寸，用户说明为主、命令标签为辅。
- `/init 这个技能是干嘛的` 等**询问技能用途**的输入改为普通对话发送，不再误触发技能执行。

## 0.0.96

- 修复斜杠命令后附加文字（如 `/init 这个技能是干嘛的`）在气泡中丢失：保留并展示 `slashArguments`，技能类命令也从 text 分片读取用户说明。

## 0.0.95

- 修复首次发斜杠命令时运行中气泡为空：流式刷新不再用 Server 空消息覆盖已补全的 `/命令` 展示，并在发送前即记录待发斜杠命令。

## 0.0.94

- 修复技能用户气泡仍为空：发送时记录斜杠命令，刷新后套用到**最近**的空用户消息，并持久化避免重复刷新丢失。

## 0.0.93

- 修复部分技能（如 `/view`）用户气泡仍为空：从 subtask.agent / description 推断命令名，并用 `slashCommand` 字段兜底渲染。

## 0.0.92

- 技能 / 斜杠命令用户气泡只展示 `/命令` 胶囊与简短说明，不再铺开技能 prompt 模板正文。

## 0.0.91

- 修复斜杠命令 / 技能（如 `/view`、`/review`）用户气泡为空：从 Server 的 `subtask` 分片还原 `/<命令>` 展示。

## 0.0.90

- `/` 斜杠命令补全改为与 OpenCode Server 全量同步，不再只显示 help/debug/mcps 三条。
- 去掉补全列表左侧「OPENCODE CLI / 命令」来源标签，统一为命令名 + 描述。
- 已在插件 UI 提供的命令（agents、models、connect、editor、exit、diff、sessions 等）从补全中排除。

## 0.0.89

- 「回到底部」改为向下箭头图标；提高显示阈值并加延迟，避免流式输出自动滚底时按钮闪烁。

## 0.0.88

- 工具展开区（Read/Write 文档、代码块）限制最大高度，超出部分在区块内滚动。
- 上滑查看历史时出现「回到底部」按钮；仅在贴近底部时自动跟随新消息滚底。

## 0.0.87

- 提升 Webview 正文可读性：字号 12px→13px、行高加大，次要文字不再用过淡的 description 色，代码注释对比度提高。

## 0.0.86

- Read `.md` 文件：去掉 `1:` / `1|` 行号后渲染为 Markdown 预览。
- Write/Edit `.md` 文件：保持语法高亮代码视图，不再渲染为 Markdown。

## 0.0.85

- 授权与提问确认改为队列展示：同一时间只显示一条，标题显示 `(1/N)`，处理完当前项后再出现下一条。

## 0.0.84

- Glob/Grep 结果：计数与路径分行展示，路径可点击打开；修复「1 条结果」与路径粘在一起导致无法点击的问题。
- 展开区去重：Glob/Grep 有结果时不再重复展示搜索条件；TodoWrite 只展示一份待办清单。

## 0.0.83

- 新对话首条消息：创建会话前先展示用户气泡；`adoptSession` 与模型目录加载改为后台执行，不阻塞发送。
- 连接就绪：`phase=ready` 提前到会话列表加载完成，模型目录在后台刷新。
- 流式与消息刷新节流略收紧（32ms→16ms、50ms→24ms），首条回复体感更快。
- Edit/Write 工具展开：变更行红底、新增行绿底（跟随 VS Code diff 配色），便于一眼看出改动。

## 0.0.82

- Read 目录结果：解析 `<entries>` XML，展示为可点击的文件/文件夹列表，不再显示原始 XML 标签。

## 0.0.81

- Edit/Write 展开：识别 `content` 等字段并展示写入/变更内容；路径可点击跳转；隐藏 `Edit applied successfully` 等无信息量成功短句。

## 0.0.80

- 助手回复正文支持 Markdown 渲染（标题、列表、围栏代码块及语法高亮等）。

## 0.0.79

- Read 工具展开时不再重复显示文件路径（input 仅含路径时跳过，只展示 output）。
- 工具展开区的文件路径可点击，在 VS Code 主编辑区打开对应文件。

## 0.0.78

- 修正输入框位置：恢复 `height: 100%` 布局链，Composer 重新固定底部。

## 0.0.77

- 修复 Webview 白屏：`app.js` 改为 `type="module"` 加载（Vite 产物含 `import.meta`，普通 script 标签会 SyntaxError）。
- 移除违反 CSP 的内联样式，启动占位样式并入 `app.css`。

## 0.0.76

- 修复 Webview 白屏：强制单文件 `app.js` 打包（VS Code Webview 无法可靠加载动态分片）；highlight.js / marked 改为首次使用时才加载，不阻塞首屏。
- 启动时显示「OpenCode 正在加载…」占位；脚本失败时展示可读错误信息。
- 修正 Webview 高度塌陷（`min-height` 兜底）。

## 0.0.75

- 修复安装后 Webview 白屏：`PayloadBody`（含 highlight.js）改为懒加载，首屏不再同步加载高亮库；扩展热重载时正确重建 Webview surface。
- Webview CSP 允许加载懒加载脚本分片（`PayloadBody.js`、`marked` 等）。
- 加载失败时显示可读错误信息，便于排查。

## 0.0.74

- 工具展开按类型解析：TodoWrite/todowrite 显示待办清单而非原始 JSON；Read 解析 XML 输出、去掉行号并按扩展名语法高亮；`.md` 文件渲染为 Markdown（标题、列表、代码块等）。

## 0.0.71

- 修复安装 0.0.70 后 Webview 白屏：工具载荷改为仅在展开时渲染与高亮，避免历史消息加载时同步高亮阻塞主线程。
- 高亮失败时降级为纯文本，不再拖垮整个界面。

## 0.0.70

- 工具展开区支持语法高亮：Write/Edit 代码按文件类型着色，Bash 命令与 `command:` 字段高亮，配色跟随 VS Code 暗色/亮色主题。

## 0.0.69

- 修复时间线圆点遮挡文字：圆点中心对齐竖线（左缘 9px），不再压在「已完成」「思考」等状态文字上。

## 0.0.68

- 时间线改为一条贯穿容器的竖线，所有圆点（含输出蓝点）统一骑在线正中间；竖线连续不断，一次对话中途穿插正文不再断开。
- 工具展开直接展示内容原文：Write/Edit 展示写入的代码与新旧片段，Bash 展示命令原文，Read 展示路径与行范围，Glob/Grep 展示匹配模式，待办与提问展示列表；不再渲染「输入/输出」字段标签或结构化 JSON、无 `(N 字符,M 行)` 占位。

## 0.0.67

- 修复时间线圆点与文字不对齐：统一为 9px 圆点并按文字行基线对齐，移除方/菱形差异。
- 修复一次回复中途出现正文时时间线断开：连续 assistant 段（工具→正文→工具→正文）合并为同一条左竖线，不再被中途文字拆成多条时间线。
- 工具展开内容由原始 JSON 改为人性化键值展示：命令/长文本走代码块，路径内联，数组走列表，键名中文化。
- `@` 提及支持选择文件：无活动会话时回退到工作区目录，空 query 也列出文件候选项（走 OpenCode `find.files`）。
- 多文件附件发送时用户气泡统一渲染为附件卡片，不再把多余的文件引用展示成结构化文本。
- `/` 斜杠命令与 `!shell` 发送时，消息气泡只展示命令名胶囊 + 参数摘要，不再铺出整段命令裸文本。

## 0.0.45

- 当前目录没有模型记录时，首次加载回退到全局最近的模型会话，兼容 CLI 在其他项目中选择模型的情形。

## 0.0.44

- 配置未指定默认模型时，从当前目录最近一次 OpenCode 会话恢复模型与智能体，首次加载不再等到发送后才更新。
- 用实时 Provider 模型集合过滤 v2 目录中的已下线模型，避免选择后出现 `ModelNotFound`。
- 会话失败时展示 OpenCode 返回的具体错误，不再统一隐藏为「会话执行失败」。

## 0.0.42

- 初次加载即从 OpenCode 配置（`opencode.json` 的 `model`）同步默认模型与智能体，底栏不再先显示「默认模型」。
- 编写区切换模型/智能体时写回 OpenCode 配置，与 CLI 选择效果一致，重装插件后仍保留。

## 0.0.41

- Webview 全局细滚动条：透明轨道、圆角滑块、去除箭头；模型弹层列表同步优化。

## 0.0.40

- 修复首次对话误报「模型变体已不可用」：切换模型时不再自动套用变体，无效变体静默恢复为默认。

## 0.0.39

- 修复新建会话时模型/智能体选择被重置：新会话沿用上次编写区偏好，并同步到 OpenCode Server。

## 0.0.38

- 模型弹层供应商列表仅展示已配置且已连接的渠道；未连接的不再出现在选择列表中，可通过「连接供应商」添加。

## 0.0.37

- 说明官方 Zen（按量）与 Go（月订）两个独立渠道；模型弹层按供应商分组，并补充渠道说明文案。
- 拉取目录时合并 Integration 列表，未连接的 OpenCode Go 等渠道也会出现在供应商列表中。

## 0.0.36

- 加强智能体/模型弹层未选中项的悬停反馈（天蓝描边与浅底），避免看起来像不可点击。
- 模型目录展示未解锁的付费模型，并标注「需订阅」；点击可打开图形化连接供应商流程。
- 「连接供应商」改为 VS Code 向导（QuickPick / 输入框 / 浏览器 OAuth），通过 OpenCode v2 Integration API 完成认证；仅在 API 不可用时回退终端 `opencode auth login`。

## 0.0.33

- 重构编写区为 Cursor 式底栏：左下智能体与模型药丸，右下添加上下文与发送；上下文芯片移至输入框上方。
- 美化底栏弹层：智能体/模型/上下文统一为 VS Code 风格自定义菜单，互斥展开，去除原生下拉。
- 修复 v2 目录接口 `location` 传参错误，智能体列表应正确展示 `build` 与 `plan` 等主智能体。
- 移除编写区顶部临时附件提示条。
- 底栏弹层改为 Portal + fixed 定位，避免被输入框 `overflow` 裁切。
- 输入区固定底部；📎 点击直接打开文件选择器；芯片仅显示文件名；图片预览改用 data URI；智能体描述中文化。
- 模型弹层改为跳转 opencode.json 与「连接供应商」，不再误导至 VS Code 扩展设置。

## 0.0.32

- 修复 Webview 黑屏：补全初始状态中的 `composerSuggestions` 字段，并增加渲染错误边界。

## 0.0.31

- 实现图片、截图与 PDF 附件：支持选择、拖放与粘贴；发送前展示类型、大小与预览；按模型输入模态校验；剪贴板附件写入扩展私有临时目录并在移除/发送/过期时清理。

## 0.0.30

- 实现斜杠命令、`@` 提及与 Shell 输入：输入 `/` 或 `@` 打开可键盘导航的补全面板；斜杠命令与 `!shell` 分别走 OpenCode 原生命令与 Shell 接口；文件/符号提及自动添加上下文项。

## 0.0.29

- 实现编辑器上下文：自动同步选区、手动添加文件/文件夹/Problems/Git Diff/终端输出，发送时转换为 OpenCode 消息片段；新增编辑器右键命令。

## 0.0.28

- 实现动态模型供应商、模型、变体与智能体选择：目录来自 OpenCode 接口，选择随会话同步，发送消息时附带所选参数；目录变更时自动刷新并校验失效选择。

## 0.0.27

- 实现安全的会话分享：分享前二次确认、复制链接、取消分享；调用 OpenCode 原生分享接口；分享能力禁用时自动隐藏相关操作；诊断输出脱敏分享链接。

## 0.0.26

- 修复输入框在连续长文本时横向撑开的问题：限制容器宽度并强制换行，改为纵向增高。

## 0.0.25

- 输入框改为上下分区布局：上方输入区、下方工具栏，外观为一体；内容随输入增高，达上限后在输入框内显示轻量滚动条。

## 0.0.24

- 输入框改为 Codex 风格：发送按钮移入对话框右下角，天蓝色图标按钮；去掉 textarea 缩放斜杠。

## 0.0.23

- 修复当前会话对话结束时仍弹出「OpenCode 后台任务已完成」的问题：仅在其他会话后台完成时提示。

## 0.0.22

- 修复连接卡在「正在连接 OpenCode Server…」且重试无反应：重试会打断挂起的连接并重新发起，不再排队等待前一次。
- 为连接与健康检查增加超时（30 秒 / 单次健康检查），避免网络或进程异常时无限等待。

## 0.0.21

- 修复思考内容混入回复正文：仅将 `text` 分片的增量拼入消息，`reasoning` 分片不再误写入正文。
- 修复消息结束后圆点仍闪烁：文本流结束时立即落盘并清除 `streaming`；服务端已完成后始终以服务端正文为准。
- 会话进入 idle 时触发消息刷新，避免状态滞后。

## 0.0.20

- 修复消息输出完成后左侧圆点仍闪烁的问题：服务端已结束时正确清除 `streaming` 状态。
- 监听 `session.next.text.ended` 触发消息刷新，并在刷新时清理已完成的待处理增量。

## 0.0.19

- 修复重装后长期停留在「正在准备连接」的问题：页面就绪后立即同步状态，不再等待初始化完成才首次投递。
- 连接未就绪时（准备中、检测 CLI、连接中等）均显示「重试」按钮；页面加载后补发一次 `surface-ready` 以防消息丢失。

## 0.0.18

- 修复对话输出不流式显示的问题：监听 `message.part.delta` 与 `session.next.text.delta` 事件，实时追加文本增量。

## 0.0.17

- 修复重装或重开侧边栏后可能一直卡在「正在连接 OpenCode Server…」的问题：页面就绪前缓存状态，就绪后等待初始化完成再投递。
- 连接中状态增加「重试」按钮，便于手动恢复。

## 0.0.16

- 修复流式回复被防抖推迟到结束后才一次性显示的问题，改为节流实时刷新。
- 流式指示改为仅左侧时间线圆点闪烁，移除消息右侧动态点。

## 0.0.15

- 修复短用户消息悬停时，「重新发送」「编辑」操作栏被挤成竖排的问题。

## 0.0.14

- 会话导航改为 Codex 风格工具栏：返回、标题、更多操作、历史、设置与新建会话；会话列表移至历史面板。
- 修复 Webview 重载后可能一直显示「正在连接」的问题；页面就绪时自动同步状态并恢复中断的会话加载。
- 连接状态栏区分「正在连接 Server」与「已连接 · 正在加载会话」。
- `npm run package` 打包前自动递增补丁版本号。

## 0.0.13

- 修复历史会话加载时，部分 Webview 环境不支持自动滚动接口而导致整个聊天页面空白的问题；自动滚动不可用时安全跳过。
- 用户消息改为紧凑的右侧中性气泡；OpenCode 回复改为左侧内容流与轻量时间线标记。历史消息的编辑与重新发送仅在悬停或键盘聚焦用户气泡时显示。

## 0.0.12

- 移除同项目写入租约、跨窗口排队和“等待写入权”状态；不再因同项目的另一会话而阻止发送。
- 切换会话前中断当前窗口发起的运行任务，再加载目标会话；外部客户端运行的会话仍仅进入跟随模式，不会被中止。

## 0.0.11

- 保留顶部连接状态栏；消息区调整为右侧紧凑用户气泡与左侧内容流式 OpenCode 回复，降低卡片边框干扰。
- 历史消息的“编辑”和“重新发送”仅在鼠标悬停或键盘聚焦用户气泡时显示，编辑表单打开后保持可见。

## 0.0.10

- 修复关闭并重新打开聊天视图后，页面可能错过运行时状态而一直显示“正在连接”的问题；页面就绪后会重新获取当前会话与连接状态，不会重连或中断任务。

## 0.0.9

- 会话交互改为“单会话优先”：移除插件创建 OpenCode 分支、父子会话导航及相关 SDK 调用。
- 历史消息的“重新发送”和“编辑后发送”均追加到当前会话；原消息保持不变，便于追溯且不会增加会话数量。

## 0.0.8

- 增加受管本地、外部同文件系统和外部远程三种连接拓扑；非回环地址强制 HTTPS，远程文件能力按路径验证结果安全降级。
- 外部服务密码继续只存入 VS Code 密钥存储，未信任工作区不读取工作区连接设置或项目配置。
- 增加按项目和工作树协调的跨窗口写入租约，支持所有者、心跳、过期恢复、不同项目并行和已证明只读任务并行。
- 增加运行中、等待写入权、跟随中、等待授权、已完成、失败和已中断等会话状态，并提供无敏感内容通知。
- 增加会话筛选、重命名、模态确认删除、父子会话导航，以及基于 OpenCode 原生分支的重试、从此处继续和编辑历史提示。
- 串行处理 OpenCode 事件并保护终态，避免重复或延迟事件造成状态倒退和重复通知。
- 保持模块单一职责，拆分连接拓扑、能力探测、写入租约、会话协调、会话映射和 SDK 错误归一化。

## 0.0.7

- 修复加载会话列表失败后界面永久停留在“正在连接”的问题，失败时释放本地 Server 并显示实际错误。
- 修复右侧辅助侧边栏容器标识不符合 VS Code 清单规则，导致聊天视图被错误放入资源管理器的问题。
- 初始状态显示“正在准备连接”，不再误报“连接失败”。
- 增加不记录会话内容、消息、草稿和连接密码的分阶段连接诊断日志。

## 0.0.6

- 打开聊天后无需先点击新建会话，输入并发送首条消息时自动创建 OpenCode 会话。
- 仅在真正发送时创建会话，避免打开面板产生空会话。
- 没有工作区时在首次发送流程中选择目录；取消或失败会保留草稿并显示错误。
- 更新空状态引导，并增加首次发送的 Runtime 与界面行为测试。

## 0.0.5

- 新建会话时不再写入固定的“新会话”标题，由 OpenCode 在首轮对话后自动命名。
- 保持监听 OpenCode 的会话更新事件，自动刷新生成后的标题。
- 增加创建请求和标题更新行为回归测试。

## 0.0.4

- 修复中文输入法拼音和候选词在输入框中重复、回退或被旧状态覆盖的问题。
- 输入法组合期间不再向 Extension Host 发送半成品草稿，确认候选词后再同步。
- 输入法候选词确认使用的 Enter 不再误触发消息发送。
- 增加中文组合输入和异步旧草稿回显的组件回归测试。

## 0.0.3

- 修复 Windows 中终端可以运行 `opencode`，但 Cursor/VS Code 扩展宿主误报 CLI 未安装的问题。
- 独立封装跨平台命令运行器，支持 npm `.cmd` shim、PowerShell `.ps1` 路径和原生可执行文件。
- 增加 Windows 命令 shim 回归测试与真实本地 Server 冒烟测试。

## 0.0.2

- 默认把聊天视图放在右侧辅助侧边栏，保留左侧资源管理器。
- 增加编辑器标题栏和视图工具栏入口，不再依赖命令面板启动。
- 使用独立的社区版命令、设置与视图标识，避免和官方 OpenCode 插件冲突。

## 0.0.1

- 建立扩展外壳、CLI 健康检查和安全本地 Server 生命周期。
- 实现基础流式会话与侧边栏/编辑器双形态。
